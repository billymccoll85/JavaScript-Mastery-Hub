let x;

// Square root
x = Math.sqrt(16); // Returns the square root of a number

// Absolute value
x = Math.abs(-10); // Returns the absolute value of a number

// Round
x = Math.round(3.7); // Rounds a number to the nearest integer

// Round up
x = Math.ceil(2.3); // Rounds a number up to the nearest integer

// Round down
x = Math.floor(5.9); // Rounds a number down to the nearest integer

// Exponent
x = Math.pow(3, 4); // Returns the value of a number raised to the power of another number

// Minimum number
x = Math.min(7, 2, 9); // Returns the smallest number among the given arguments

// Maximum number
x = Math.max(6, 1, 8); // Returns the largest number among the given arguments

// Get a random number/decimal between 0 and 1
x = Math.random(); // Returns a random number between 0 (inclusive) and 1 (exclusive)

// Get a random number between 1 and 100
x = Math.floor(Math.random() * 100 + 1); // Returns a random integer between 1 and 100

console.log(x);

// Maximum number
x = Math.max(4, 5, 3);

// Get a random number/decimal between 0 and 1
x = Math.random();

// Get a random number between 1 and 100
x = Math.floor(Math.random() * 100 + 1);

console.log(x);
