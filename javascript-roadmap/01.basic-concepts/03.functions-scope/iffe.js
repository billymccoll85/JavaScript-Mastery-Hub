// IIFE (Immediately Invoked Function Expression)
(function() {
    // Code inside this function will be executed immediately
    // and will not pollute the global scope

    // Add your code here

    const message = "Hello, I'm inside an IIFE!";
    console.log(message);

})();

// Any variables or functions declared inside the IIFE
// are not accessible outside of it
