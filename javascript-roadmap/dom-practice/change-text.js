// Get the element with the id "myElement"
const element = document.getElementById("myElement");

// Change the text of the element
element.textContent = "Hello, world!";

// You can also use innerHTML to change the HTML content of the element
// element.innerHTML = "<strong>Hello, world!</strong>";

// Use textContent instead of innerHTML to change the HTML content of the element
// textContent is specifically designed for handling text content, while innerHTML can handle both text and HTML content
