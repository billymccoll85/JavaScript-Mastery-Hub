import React from 'react';

const TimerDisplay = ({ time }) => {
  // Formatting time for display
  const formatTime = () => {
    // same formatTime function as earlier
  };

  return <div className="time-display">{formatTime()}</div>;
};

export default TimerDisplay;
